echo "# Fraud Detection App

A machine learning web app built with Streamlit to detect fraudulent transactions.

## How to Run
pip install -r requirements.txt
streamlit run fraud_detection.py
" > README.md

git add README.md
git commit -m "Add README"
git push
